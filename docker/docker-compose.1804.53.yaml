version: "3"

services:

  runtime-setup:
    image: async-http-client:18.04-5.3
    build:
      args:
        ubuntu_version: "bionic"
        swift_version: "5.3"

  test:
    image: async-http-client:18.04-5.3
    environment:
      - TEST_DISCOVERY=--enable-test-discovery

  shell:
    image: async-http-client:18.04-5.3
